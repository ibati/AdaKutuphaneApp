@using AdaKutuphaneApp.Models.Entity
@model List<tblPersoneller>

@{
    ViewBag.Title = "Personel Listesi";
    Layout = "~/Views/Shared/Layout.cshtml";
}

<!-- Content Header (Page header) -->
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Personeller</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="#">Anasayfa</a></li>
                    <li class="breadcrumb-item active">Personeller</li>
                </ol>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>

<!-- Main content -->
<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h3 class="card-title"><button onclick="window.location.href='/Personeller/PersonelEkle'" type="button" class="btn btn btn-primary"><i class="fas fa-edit"></i> Yeni Personel Ekle</button></h3>
                    </div>
                    <div class="card-body">
                        <table id="datatable1" class="table table-bordered table-hover">
                            <thead>
                                <tr>
                                    <th width="5%">ID</th>
                                    <th width="40%">Personel Adı</th>
                                    <th width="40%">Personel Soyadı</th>
                                    <th width="15%">İşlemler</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var personel in Model)
                                {
                                    <tr>
                                        <td>@personel.ID</td>
                                        <td>@personel.AD</td>
                                        <td>@personel.SOYAD</td>
                                        <td>
                                            <a href="/Personeller/PersonelGetir/@personel.ID"><button type="button" class="btn btn-sm btn-outline-primary"><i class="fas fa-edit"></i> Güncelle</button></a>
                                            @Html.ActionLink("Sil", "PersonelSil", new { id = personel.ID }, new { @class = "btn btn-sm btn-outline-danger", onclick = "return confirm('Silmek istediğinize emin misiniz?')" })
                                        </td>
                                    </tr>
                                }
                            </tbody>
                            <tfoot>
                                <tr>
                                    <th width="5%">ID</th>
                                    <th width="40%">Personel Adı</th>
                                    <th width="40%">Personel Soyadı</th>
                                    <th width="15%">İşlemler</th>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                    <!-- /.card-body -->
                </div>
                <!-- /.card -->
            </div>
            <!-- /.col -->
        </div>
        <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
</section>
<!-- /.content -->
